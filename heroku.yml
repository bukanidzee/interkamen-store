setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
  # config:
  #   env_file: .env
    # S3_BUCKET: my-example-bucket
build:
  docker:
    nginx: heroku_config/nginx/Dockerfile
    api: heroku_config/api/Dockerfile
  config:
    REACT_APP_API_ORIGIN: ${REACT_APP_API_ORIGIN}
# release:
#   command:
#     - ./deployment-tasks.sh
#   image: worker
# run:
#   : bundle exec puma -C config/puma.rb
#   worker: python myworker.py
